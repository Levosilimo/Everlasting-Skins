name: CI v1
on:
  push:
    branches: ["1.21"]
env:
  GRADLE_OPTS: -Dorg.gradle.daemon=false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build"
  e2e:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "E2E"
