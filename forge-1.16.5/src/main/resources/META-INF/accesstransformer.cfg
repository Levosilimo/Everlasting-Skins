# Access transformers for the 1.16.5 lane.
#
# The 1.21 entries copied over referenced 1.21 SRG names (m_140331_ etc.)
# and were removed: FG 5.1 applies this file by SRG name, and stale entries
# would fail the reobfuscation step. The 1.16.5 tracker entry points
# (ServerChunkCache.removeEntity/addEntity) appear to be public on 1.16.5
# already — re-verify during the compile-fix pass and re-add SRG names if
# the decompiled jar shows otherwise.
